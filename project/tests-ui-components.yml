# SPDX-FileCopyrightText: 2026 Kaito Udagawa <umireon@kaito.tokyo>
# SPDX-License-Identifier: Apache-2.0

targets:
  LDTXAppUIComponentTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - Tests/LDTXAppUIComponentTests
    dependencies:
      - target: LDTXApp
      - target: LDTXAppletSupport
      - target: LDTXWorkspaceApplet
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: tokyo.kaito.ldtx.LDTXAppUIComponentTests
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_VERSION: "6.0"
        SWIFT_OBJC_INTEROP_MODE: objcxx
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/LDTX.app/Contents/MacOS/LDTX"
        BUNDLE_LOADER: "$(TEST_HOST)"

schemes:
  LDTXAppUIComponentTests:
    build:
      targets:
        LDTXAppUIComponentTests: all
    test:
      config: Debug
      targets:
        - LDTXAppUIComponentTests
